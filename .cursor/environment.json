{
  "name": "gis-toolbox-dist-preview",
  "install": "npm install",
  "ports": [
    {
      "name": "dist-preview",
      "port": 4173
    }
  ],
  "terminals": [
    {
      "name": "vite-preview",
      "command": "npm run build && npm run preview -- --host 0.0.0.0 --port 4173 --strictPort",
      "description": "Builds the app and serves the dist output via Vite preview for smoke tests."
    }
  ]
}
