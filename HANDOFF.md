# Agent handoff

Keep this file current so the next session can continue without re-discovery.

## Latest

- **Date**: 2026-06-05
- **Goal**: Pin `main` to the **Final refactor plan commit** baseline (`0aa40f0`).
- **Branch**: `main`
- **Commit**: `0aa40f0` ("Final refactor plan commit")
- **Verification**: `npm test` ✅ (120), `npm run build` ✅
- **Note**: Post-refactor fix attempts (#21–#24) and rollback PR #25 are no longer on `main`; history was reset to this checkpoint per owner request.

---

## Prior (M4–M7 continuation — 2026-06-05)

- **Goal**: Complete **M4**, **M5**, **M6**, and continue **M7** with React host infrastructure + first tool-dialog migration while preserving rollback safety.
- **Branch**: `main`
- **Fix**:
  - Re-validated baseline on current working tree:
    - `npm test` and `npm run build` are green before M4 edits.
  - Added first M4 extraction boundary:
    - `js/map/map-service.js` (`createMapService`, delegating lifecycle + core map ops to current `mapManager`).
    - `js/map/map-feature-flags.js` (new map island flag resolver).
    - **Default remains legacy map path**; React map is opt-in for rollback safety.
  - Added initial React map island:
    - `react/map/MapView.jsx` (owns map init/destroy through `mapService`).
    - `react/map/mountMapView.jsx` (mount helper + readiness promise).
    - `js/app.js` now routes map init through:
      - legacy/default: `mapService.init('map-container')`
      - opt-in M4 path: dynamic mount of `<MapView>` via `mapReactView` flag.
  - Added styling support for map island host:
    - `css/main.css`: `.map-react-view-host` fill container sizing.
  - Added tests for new M4 slice:
    - `tests/map-feature-flags.test.js`
    - `tests/map-service.test.js`
  - Continued incremental `MapService` adoption in `js/app.js` (without changing shell path):
    - switched several low-risk calls from `mapManager` → `mapService`:
      - map resize hooks (boot/panel-collapse/mobile-map),
      - import/photo/workflow/draw add/remove/toggle paths,
      - basemap/2D-3D toggles,
      - layer reorder sync,
      - style apply + field-refresh map updates.
  - Expanded `MapService` wrapper surface in `js/map/map-service.js`:
    - style helpers (`getLayerStyle`, `setLayerStyle`),
    - basemap state (`getCurrentBasemap`),
    - fence/search helpers (`hasImportFence`, `clearImportFence`, `startImportFenceDraw`, `getImportFenceEsriEnvelope`, `getSearchLatLng`, `clearSearchMarker`),
    - selection helpers (`isSelectionMode`, `getSelectedIndices`, `getSelectedFeatures`, `getSelectionCount`, `enter/exit/clear/selectAll/invert`),
    - map interaction helpers (`startPointPick`, `startTwoPointPick`, `startRectangleDraw`, `startSketchPolygon`, `startSketchCirclePolygon`, `showInteractionBanner`, `showTempFeature`),
    - layer lookup helper (`getLayerRecord`).
    - layer-id + adapter state helpers (`getLayerIds`, `getLayerStyles`, `setCurrentBasemap`, `is3DEnabled`, `set3DEnabled`).
    - popup/orbit helpers (`hasPopupHits`, `cyclePopup`, `getActivePopupHit`, `closePopup`, `findFeaturesNearClick`, `showMultiPopup`, `showPopup`, `isOrbiting`, `startCameraOrbit`, `stopCameraOrbit`).
    - compatibility getters for legacy-style consumers (`map`, `dataLayers`).
  - Migrated `js/app.js` to consume these wrappers, removing direct `mapManager` method calls.
    - `js/app.js` now has no direct `mapManager.*` usage and no direct `mapManager` import; it goes through `mapService` for map operations and routes compatibility hooks via `mapService`:
      - dual-screen facade installed with `installDualScreenMapFacade(mapService.getLegacyMapManager())`,
      - export style lookup wired with `setExportMapManager(mapService)`.
    - widget dependency injection now passes `mapService` (Spatial Analyzer / Bulk Update / Proximity Join).
  - Migrated dual-screen modules to `mapService`:
    - `js/dual-screen/secondary-client.js` now uses `mapService` for popup bridge, orbit actions, and fence commands.
    - `js/map-window.js` now uses `mapService` for snapshot apply, layer sync, viewport fits, and map chrome toggles.
    - `js/dual-screen/coordinator.js` now uses `mapService` for map lifecycle restore, snapshot chrome state, and bounds access.
  - Migrated draw subsystem boundary:
    - `js/map/draw-manager.js` now uses `mapService` instead of importing `map-manager` directly for selection, interaction cancel, rectangle draw delegation, and highlight/lookup calls.
  - Updated widget dependency injection in `js/app.js`:
    - Spatial Analyzer, Bulk Update, and Proximity Join now receive `mapService` as their map dependency.
  - Continued M4 extraction for remaining mapManager-centric widgets:
    - `js/widgets/bulk-update.js` now resolves map dependency through `mapService` first (`this.mapService`), with a legacy fallback to `this.mapManager` during overlap.
    - `js/widgets/spatial-analyzer.js` now resolves map dependency through `mapService` first (`this.mapService`), with a legacy fallback to `this.mapManager` during overlap.
    - `js/widgets/proximity-join.js` now resolves map dependency through `mapService` first (`this.mapService`), with a legacy fallback to `this.mapManager` during overlap.
    - These widgets now route map interactions through `MapService` wrappers where available (`startRectangleDraw`, `startSketchPolygon`, `startSketchCirclePolygon`, `showInteractionBanner`, `getSelectionCount`, `getSelectedIndices`, `refreshLayerData`, `addLayer`, `getLayerRecord`, `getMap`) and only use raw MapLibre calls for transient preview/highlight rendering.
    - `js/app.js` now injects `mapService` via the explicit `mapService` widget property for:
      - `openSpatialAnalyzer()`
      - `openBulkUpdate()`
      - `openProximityJoin()`
  - Dual-screen layout helper cleanup for M4 consistency:
    - `js/dual-screen/layout.js` `scheduleMapResizeAfterLayout()` now accepts map-service-shaped dependencies (`resize` + optional `getMap`) and schedules an additional resize on map `load` when available.
    - `js/dual-screen/coordinator.js` no longer duplicates `map.once('load', ...)` resize scheduling; this is now centralized in `scheduleMapResizeAfterLayout(mapService)`.
    - Added regression coverage in `tests/dual-screen-layout.test.js` for the map-load resize path.
  - Export compatibility adapter cleanup:
    - `js/export/exporter.js` now stores a generic map API adapter (`_mapApi`) instead of a `mapManager`-named variable.
    - `setExportMapManager(...)` remains the compatibility entrypoint and accepts either `mapService` or legacy map-manager shaped objects (uses `getLayerStyle`).
  - M4 boundary completion pass:
    - `js/app.js` no longer imports or references `mapManager` directly.
    - compatibility hooks are now attached via `mapService`:
      - `installDualScreenMapServiceDecorator(mapService, dualScreenCoordinator)` (current path; replaced legacy dual-screen map facade)
      - `setExportMapManager(mapService)`
    - `rg \bmapManager\b js/` now reports only:
      - dual-screen coordinator/decorator overlap modules (`js/dual-screen/coordinator.js`, `js/dual-screen/dual-screen-map-service.js`),
      - adapter implementation: `js/map/map-service.js` + `js/map/map-manager.js`,
      - overlap safety fallbacks in migrated widgets (`mapService` first, `mapManager` fallback).
  - Completed M5 (left panel React island):
    - Added left-panel feature flag:
      - `js/ui/left-panel-feature-flags.js` (default ON; rollback via `leftPanelReact=0` query/localStorage/global).
      - `tests/left-panel-feature-flags.test.js`.
    - Added React left-panel island:
      - `react/panels/LeftPanel.jsx` (`LayerListPanel`, `FieldListPanel`, `DataPrepToolsPanel`).
      - `react/panels/mountLeftPanel.jsx` (multi-root mount for `#layer-list`, `#field-list`, `#dataprep-tools`).
    - Updated `js/app.js` for M5 path:
      - mounts React left panel during boot when feature flag is enabled,
      - falls back to legacy left panel if React mount fails,
      - `refreshUINow()` routes left panel refresh through React render when enabled,
      - legacy render functions are still retained behind the rollback path.
    - Extracted reusable left-panel actions in `js/app.js` and reused them in both React island and `window.app`:
      - `setActiveLayerAndRefresh`, `toggleLayerVisibilityAndRender`, `zoomToLayer`, `removeLayerWithConfirm`.
  - Completed M6 (right panel React island):
    - Added right-panel feature flag:
      - `js/ui/right-panel-feature-flags.js` (default ON; rollback via `rightPanelReact=0` query/localStorage/global).
      - `tests/right-panel-feature-flags.test.js`.
    - Added React right-panel island:
      - `react/panels/RightPanel.jsx` (output schema, export controls, AGOL readiness, data preview, style-panel host).
      - `react/panels/mountRightPanel.jsx` (mount helper for `#output-panel-content`).
    - Updated `js/app.js` for M6 path:
      - mounts React right panel during boot when feature flag is enabled,
      - falls back to legacy right panel if React mount fails,
      - `renderOutputPanel()` routes to React render when enabled and preserves legacy path as rollback,
      - style-panel behavior remains wired through existing `buildStylePanel()` + `bindStylePanel(...)` logic for parity.
  - Started M7 with React toast-host migration (API-compatible):
    - Added toast feature flag:
      - `js/ui/toast-feature-flags.js` (default ON; rollback via `toastReact=0` query/localStorage/global).
      - `tests/toast-feature-flags.test.js`.
    - Added React toast host:
      - `react/ui/ToastHost.jsx`.
      - `react/ui/mountToastHost.jsx`.
    - Updated `js/ui/toast.js`:
      - preserved `showToast` / `showErrorToast` API,
      - added subscriber-based toast event flow for React host (`subscribeToasts`, `dismissToast`),
      - kept legacy DOM rendering path as fallback when no subscriber is mounted.
    - Updated `js/app.js`:
      - mounts React toast host during boot when enabled,
      - falls back silently to legacy toast rendering if React toast host mount fails.
    - Added toast event tests:
      - `tests/toast-events.test.js` validates add/remove event flow and error-toast formatting through the subscriber path.
  - Continued M7 with React modal-host migration (API-compatible):
    - Added modal feature flag:
      - `js/ui/modal-feature-flags.js` (default ON; rollback via `modalReact=0` query/localStorage/global).
      - `tests/modal-feature-flags.test.js`.
    - Added React modal host:
      - `react/ui/ModalHost.jsx`.
      - `react/ui/mountModalHost.jsx`.
    - Updated `js/ui/modals.js`:
      - preserved `showModal` / `confirm` / `showProgressModal` API,
      - added subscriber-driven modal/progress event flow for React host (`subscribeModalEvents`, `dismissModal`, `dismissProgressModal`, `triggerProgressCancel`),
      - kept legacy DOM modal rendering as fallback when no subscriber is mounted.
    - Updated `js/app.js`:
      - mounts React modal host during boot when enabled,
      - falls back to legacy modal rendering if React modal host mount fails.
    - Added modal event tests:
      - `tests/modal-events.test.js` validates modal show/remove/resolve behavior and progress cancel/update/remove flow.
  - Continued M7 with first React tool-dialog migration:
    - Added tool-dialog feature flag:
      - `js/ui/tool-dialog-feature-flags.js` (default OFF; opt-in via `toolDialogsReact=1` query/localStorage/global).
      - `tests/tool-dialog-feature-flags.test.js`.
    - Added React tool-dialog components:
      - `react/tools/DistanceToolDialog.jsx`.
      - `react/tools/mountDistanceToolDialog.jsx`.
      - `react/tools/PointToLineDistanceDialog.jsx`.
      - `react/tools/mountPointToLineDistanceDialog.jsx`.
      - `react/tools/BearingToolDialog.jsx`.
      - `react/tools/mountBearingToolDialog.jsx`.
      - `react/tools/DestinationToolDialog.jsx`.
      - `react/tools/mountDestinationToolDialog.jsx`.
      - `react/tools/AlongToolDialog.jsx`.
      - `react/tools/mountAlongToolDialog.jsx`.
      - `react/tools/BufferToolDialog.jsx`.
      - `react/tools/mountBufferToolDialog.jsx`.
      - `react/tools/SimplifyToolDialog.jsx`.
      - `react/tools/mountSimplifyToolDialog.jsx`.
      - `react/tools/ClipExtentDialog.jsx`.
      - `react/tools/mountClipExtentDialog.jsx`.
      - `react/tools/BboxClipDialog.jsx`.
      - `react/tools/mountBboxClipDialog.jsx`.
      - `react/tools/BezierSplineDialog.jsx`.
      - `react/tools/mountBezierSplineDialog.jsx`.
      - `react/tools/PolygonSmoothDialog.jsx`.
      - `react/tools/mountPolygonSmoothDialog.jsx`.
      - `react/tools/LineOffsetDialog.jsx`.
      - `react/tools/mountLineOffsetDialog.jsx`.
      - `react/tools/LineSliceAlongDialog.jsx`.
      - `react/tools/mountLineSliceAlongDialog.jsx`.
      - `react/tools/LineSliceDialog.jsx`.
      - `react/tools/mountLineSliceDialog.jsx`.
      - `react/tools/LineIntersectDialog.jsx`.
      - `react/tools/mountLineIntersectDialog.jsx`.
      - `react/tools/KinksDialog.jsx`.
      - `react/tools/mountKinksDialog.jsx`.
      - `react/tools/CombineFeaturesDialog.jsx`.
      - `react/tools/mountCombineFeaturesDialog.jsx`.
      - `react/tools/UnionPolygonsDialog.jsx`.
      - `react/tools/mountUnionPolygonsDialog.jsx`.
      - `react/tools/DissolveDialog.jsx`.
      - `react/tools/mountDissolveDialog.jsx`.
      - `react/tools/SectorDialog.jsx`.
      - `react/tools/mountSectorDialog.jsx`.
      - `react/tools/NearestPointDialog.jsx`.
      - `react/tools/mountNearestPointDialog.jsx`.
      - `react/tools/NearestPointOnLineDialog.jsx`.
      - `react/tools/mountNearestPointOnLineDialog.jsx`.
      - `react/tools/NearestPointToLineDialog.jsx`.
      - `react/tools/mountNearestPointToLineDialog.jsx`.
      - `react/tools/NearestNeighborAnalysisDialog.jsx`.
      - `react/tools/mountNearestNeighborAnalysisDialog.jsx`.
      - `react/tools/NearestNeighborResultsDialog.jsx`.
      - `react/tools/mountNearestNeighborResultsDialog.jsx`.
    - Updated `js/app.js`:
      - `openDistanceTool()` now has a React path behind `toolDialogsReact`,
      - React path mounts `DistanceToolDialog` inside `showModal(...)` via `onMount`,
      - `openPointToLineDistanceTool()` now has the same React modal path behind `toolDialogsReact`,
      - `openBearingTool()` now has the same React modal path behind `toolDialogsReact`,
      - `openDestinationTool()` now has the same React modal path behind `toolDialogsReact`,
      - `openAlongTool()` now has the same React modal path behind `toolDialogsReact`,
      - `openBuffer()` now has the same React modal path behind `toolDialogsReact`,
      - `openSimplify()` now has the same React modal path behind `toolDialogsReact`,
      - `openClip()` now has the same React modal path behind `toolDialogsReact`,
      - `openBboxClip()` now has the same React modal path behind `toolDialogsReact`,
      - `openBezierSpline()` now has the same React modal path behind `toolDialogsReact`,
      - `openPolygonSmooth()` now has the same React modal path behind `toolDialogsReact`,
      - `openLineOffset()` now has the same React modal path behind `toolDialogsReact`,
      - `openLineSliceAlong()` now has the same React modal path behind `toolDialogsReact`,
      - `openLineSlice()` now has the same React modal path behind `toolDialogsReact`,
      - `openLineIntersect()` now has the same React modal path behind `toolDialogsReact`,
      - `openKinks()` now has the same React modal path behind `toolDialogsReact`,
      - `openCombine()` now has the same React modal path behind `toolDialogsReact`,
      - `openUnion()` now has the same React modal path behind `toolDialogsReact`,
      - `openDissolve()` now has the same React modal path behind `toolDialogsReact`,
      - `openSector()` now has the same React modal path behind `toolDialogsReact`,
      - `openNearestPoint()` now has the same React modal path behind `toolDialogsReact`,
      - `openNearestPointOnLine()` now has the same React modal path behind `toolDialogsReact`,
      - `openNearestPointToLine()` now has the same React modal path behind `toolDialogsReact`,
      - `openNearestNeighborAnalysis()` now has a React run dialog and React results dialog behind `toolDialogsReact`,
      - shared overlay unmount watcher (`watchOverlayUnmount`) keeps React modal islands cleaned up when modal overlays close,
      - legacy HTML modal path remains unchanged as rollback/default.
  - Continued M7 high-traffic dialog migration (rollback-safe):
    - Added React tool dialogs:
      - `react/tools/PointsWithinPolygonDialog.jsx`
      - `react/tools/mountPointsWithinPolygonDialog.jsx`
      - `react/tools/CoordConverterDialog.jsx`
      - `react/tools/mountCoordConverterDialog.jsx`
    - Updated `js/app.js`:
      - `openPointsWithinPolygon()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `openCoordConverter()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
    - Kept `index.html` shell path unchanged and maintained legacy fallback for both dialogs.
  - Continued M7 import/export-adjacent dialog migration (rollback-safe):
    - Added React tool dialogs:
      - `react/tools/ImportFenceOptionsDialog.jsx`
      - `react/tools/mountImportFenceOptionsDialog.jsx`
      - `react/tools/NetworkLinksDialog.jsx`
      - `react/tools/mountNetworkLinksDialog.jsx`
      - `react/tools/KmlExportPickerDialog.jsx`
      - `react/tools/mountKmlExportPickerDialog.jsx`
    - Updated `js/app.js`:
      - `startImportFence()` active-fence options modal now supports React modal island rendering behind `toolDialogsReact` (dual-screen and primary-map variants), preserving legacy modal paths as fallback.
      - `_promptNetworkLinkAfterImport(...)` now supports a React modal island behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `_showKmzExportPicker(...)` now supports a React modal island behind `toolDialogsReact`, preserving the legacy modal path as fallback.
    - Kept `index.html` shell path unchanged and maintained legacy fallback for all migrated dialogs.
  - Continued M7 high-traffic dialog migration (rollback-safe):
    - Added React tool dialogs:
      - `react/tools/PhotoMapperDialog.jsx`
      - `react/tools/mountPhotoMapperDialog.jsx`
      - `react/tools/ArcGISImporterDialog.jsx`
      - `react/tools/mountArcGISImporterDialog.jsx`
    - Updated `js/app.js`:
      - `openPhotoMapper()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `openArcGISImporter()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - Added shared photo-processing helper flow for parity across React/legacy paths:
        - `processPhotoFilesCore(...)`,
        - `processPhotoFilesForReact(...)`,
        - legacy `processPhotoFiles(...)` now reuses the shared processing core and preserves legacy results rendering.
    - Kept `index.html` shell path unchanged and maintained legacy fallback for both dialogs.
  - Continued M7 utility modal migration (rollback-safe):
    - Added React tool dialogs:
      - `react/tools/DrawLayerChooserDialog.jsx`
      - `react/tools/mountDrawLayerChooserDialog.jsx`
      - `react/tools/MergeLayersDialog.jsx`
      - `react/tools/mountMergeLayersDialog.jsx`
    - Updated `js/app.js`:
      - `createDrawLayer()` chooser modal now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `handleMergeLayers()` modal now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
    - Kept `index.html` shell path unchanged and maintained legacy fallback for both dialogs.
  - Continued M7 data-prep modal migration (rollback-safe):
    - Added React tool dialogs:
      - `react/tools/SplitColumnDialog.jsx`
      - `react/tools/mountSplitColumnDialog.jsx`
      - `react/tools/CombineColumnsDialog.jsx`
      - `react/tools/mountCombineColumnsDialog.jsx`
    - Updated `js/app.js`:
      - `openSplitColumn()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `openCombineColumns()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
    - Kept `index.html` shell path unchanged and maintained legacy fallback for both dialogs.
  - Continued M7 data-prep modal migration (rollback-safe):
    - Added React tool dialogs:
      - `react/tools/ReplaceCleanDialog.jsx`
      - `react/tools/mountReplaceCleanDialog.jsx`
      - `react/tools/TypeConvertDialog.jsx`
      - `react/tools/mountTypeConvertDialog.jsx`
      - `react/tools/DeduplicateDialog.jsx`
      - `react/tools/mountDeduplicateDialog.jsx`
    - Updated `js/app.js`:
      - `openReplaceClean()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `openTypeConvert()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
      - `openDeduplicate()` now supports React modal island rendering behind `toolDialogsReact`, preserving the legacy modal path as fallback.
    - Kept `index.html` shell path unchanged and maintained legacy fallback for all three dialogs.
  - Started M7 widget UI migration (rollback-safe):
    - Added React widget dialog path:
      - `react/tools/ProximityJoinDialog.jsx`
      - `react/tools/mountProximityJoinDialog.jsx`
    - Added reusable join engine:
      - `js/widgets/proximity-join-engine.js`
    - Updated `js/app.js`:
      - `openProximityJoin()` now supports a React modal island path behind `toolDialogsReact`, preserving the legacy widget path as fallback.
      - React path supports source/target selection, field mappings, optional metadata fields, preview, cancellable run, progress updates, and result summary.
    - Kept `index.html` shell path unchanged and maintained legacy fallback safety.
  - Continued M7 widget UI migration (rollback-safe):
    - Added React widget dialog path:
      - `react/tools/BulkUpdateDialog.jsx`
      - `react/tools/mountBulkUpdateDialog.jsx`
    - Updated `js/app.js`:
      - `openBulkUpdate()` now supports a React modal island path behind `toolDialogsReact`, preserving the legacy widget path as fallback.
      - React path supports layer selection, selection controls (start/stop/select-all/invert/clear), selected-count polling, and multi-field bulk updates against selected features.
    - Kept `index.html` shell path unchanged and maintained legacy fallback safety.
  - Continued M7 widget UI migration (rollback-safe):
    - Added React widget dialog path:
      - `react/tools/SpatialAnalyzerDialog.jsx`
      - `react/tools/mountSpatialAnalyzerDialog.jsx`
    - Added reusable analysis engine:
      - `js/widgets/spatial-analyzer-engine.js`
    - Updated `js/app.js`:
      - `openSpatialAnalyzer()` now supports a React modal island path behind `toolDialogsReact`, preserving the legacy widget path as fallback.
      - React path supports target-layer selection, area drawing (rectangle/polygon/circle), polygon-layer area build, spatial-relation selection, matched result summary, and add-results/add-area actions.
    - Kept `index.html` shell path unchanged and maintained legacy fallback safety.
  - Started M8 draw-toolbar migration (rollback-safe):
    - Added React draw toolbar:
      - `react/map/DrawToolbar.jsx`
      - `react/map/mountDrawToolbar.jsx`
    - Updated `js/map/draw-manager.js`:
      - `showToolbar(...)` now supports a React toolbar render path when `toolDialogsReact` is enabled, preserving the legacy toolbar DOM path as fallback.
      - Existing draw interactions/tools (select/point/line/polygon/rectangle/circle/sector), hints, undo/delete/finish visibility, and close behavior are wired through shared draw-manager logic.
    - Kept `index.html` shell path unchanged and maintained legacy fallback safety.
  - Continued M8 import-flow migration (rollback-safe):
    - Added React import flow dialog:
      - `react/tools/ImportFlowDialog.jsx`
      - `react/tools/mountImportFlowDialog.jsx`
    - Updated `js/app.js`:
      - Added `openImportFlow()` to route import entry through a React modal path behind `toolDialogsReact`, preserving legacy hidden-file-input behavior as fallback.
      - React path supports drag/drop, file picker, and quick actions to ArcGIS REST, Photo Mapper, and Import Fence.
      - `btn-import` and `btn-import-mobile` now call `openImportFlow()` (fallback still uses the existing hidden input + `handleFileImport(...)` path).
      - Mobile menu/flyout "Import" actions now call `openImportFlow()` directly (instead of proxy-clicking `btn-import`), keeping a single import-entry path.
    - Kept `index.html` shell path unchanged and maintained legacy fallback safety.
  - Started M9 header/navigation migration (rollback-safe):
    - Added header feature flag:
      - `js/ui/header-feature-flags.js` (default OFF; opt-in via `headerReact=1` query/localStorage/global).
      - `tests/header-feature-flags.test.js`.
    - Added React header island:
      - `react/header/HeaderBar.jsx`
      - `react/header/mountHeaderBar.jsx`
    - Updated `js/app.js`:
      - mounts React header during boot when enabled (`headerReact`),
      - falls back to legacy header if React header mount fails,
      - React header preserves desktop header classes/IDs so existing event wiring remains compatible.
      - moved primary header action wiring to React callbacks (import/fence/photo/arcgis/draw/undo/redo/merge/logs/info) when `headerReact` is on, while legacy listener wiring remains as fallback when flag is off.
      - moved workflow toggle + basemap + 2D/3D header interactions into React-owned handlers when `headerReact` is on.
      - added shared header selection helpers (`applyBasemapHeaderSelection`, `applyDimensionHeaderSelection`) and reused them from the legacy listener path.
      - removed inline `toggleSection` dependency:
        - added delegated section-header toggle handling for explicit `data-collapsible="true"` headers,
        - removed global `window.toggleSection`,
        - extracted shared panel collapse helpers (`setPanelCollapsed`, `togglePanelCollapsed`) used by left/right panel controls.
      - added delegated `data-app-action` handling for HTML-rendered tool buttons in the left panel, removing inline action handlers from `renderDataPrepTools()`.
      - removed inline action handlers from legacy right output panel controls (`Fix All`, export buttons, `Show Data Table`) by using delegated `data-app-action` + `data-app-arg`.
      - migrated legacy layer/field/mobile panel inline handlers to delegated events:
        - `renderLayerList()`, `renderFieldList()`, `renderMobileDataPanel()`, `renderMobilePrepPanel()`, `renderMobileToolsPanel()`, `renderMobileExportPanel()`, and `updateSelectionUI()` now emit `data-*` hooks instead of inline `onclick`/`ondblclick`/`onchange`/`oninput`.
        - `setupEventListeners()` now handles delegated layer activation, inline rename gestures, field search input, and field checkbox changes.
        - `invokeWindowAppAction()` now parses boolean string args (`"true"` / `"false"`).
      - removed remaining inline remove-button handlers in modal rule builders (filter + validation) via delegated `data-remove-parent` listeners.
      - converted additional legacy property-based event wiring to `addEventListener` in high-traffic data-prep/mobile paths:
        - `openSplitColumn()`, `openCombineColumns()`, `openTemplateBuilder()`, `openReplaceClean()`, `openTypeConvert()`, `openFilterBuilder()`, `openDeduplicate()`, `openJoinTool()`, `openValidation()`.
        - `mobileShowExportModal()` now calls `doExport(...)` directly (removed `window.app` usage).
        - logs toolbar now uses `addEventListener` for search/level controls (removed `.oninput` / `.onchange` property wiring).
      - converted the remaining legacy modal/property click bindings in `js/app.js` from `.onclick` to `addEventListener` for consistency across rollback paths, including:
        - GIS tool modal fallbacks (`openDistanceTool`, `openBearingTool`, `openDestinationTool`, `openAlongTool`, `openPointToLineDistanceTool`, `openBboxClip`, `openBezierSpline`, `openPolygonSmooth`, `openLineOffset`, `openLineSliceAlong`, `openLineSlice`, `openLineIntersect`, `openKinks`, `openCombine`, `openUnion`, `openDissolve`, `openSector`, `openNearestPoint`, `openNearestPointOnLine`, `openNearestPointToLine`, `openNearestNeighborAnalysis`, `openPointsWithinPolygon`, `openCoordConverter`, and earlier buffer/simplify/clip paths),
        - utility modals/pickers (`_promptNetworkLinkAfterImport`, `_showKmzExportPicker`, `createDrawLayer`, `handleMergeLayers`, feature edit/add-field modals),
        - ArcGIS import cancel control and search add-to-layer picker.
      - added a regression guard test:
        - `tests/event-wiring-regression.test.js` asserts no reintroduction of `.onclick/.onchange/.oninput/.ondblclick` property assignments in `js/app.js`, and no inline handler attributes in `js/app.js` or `index.html`.
      - started M10 dual-screen architecture cleanup:
        - replaced primary bootstrap from legacy `map-facade` monkey-patch wiring to a MapService-level decorator install.
        - new `js/dual-screen/dual-screen-map-service.js` now decorates `mapService` operations when dual-screen is active (layer add/remove/order, fit commands, style/data sync, basemap/3D state sync, bounds/map/fence access behavior), while preserving default single-window behavior.
        - `js/app.js` now installs `installDualScreenMapServiceDecorator(mapService, dualScreenCoordinator)` instead of patching legacy map-manager directly.
        - removed obsolete `js/dual-screen/map-facade.js` runtime path.
        - updated `sw.js` cache manifest to include `./js/dual-screen/dual-screen-map-service.js` and dropped `./js/dual-screen/map-facade.js`; bumped `CACHE_VERSION` to `1.31.19`.
        - follow-up hardening:
          - `dual-screen-map-service` now keeps `resize()` pass-through active in both single-window and dual-screen modes (safe because map-manager resize already no-ops without a map).
          - removed unused `mapService.getLegacyMapManager()` seam from `js/map/map-service.js`.
      - continued M4/M10 cleanup:
        - removed remaining widget-level `mapManager` fallback seams in:
          - `js/widgets/bulk-update.js`
          - `js/widgets/spatial-analyzer.js`
          - `js/widgets/proximity-join.js`
        - widgets now resolve map interactions through `mapService` only.
      - added dual-screen decorator unit coverage:
        - `tests/dual-screen-map-service.test.js`.
      - wired logs panel close to JS listener (`#logs-close`) and removed inline close handler from `index.html`.
    - Updated `index.html`:
      - removed inline `onclick="toggleSection(this)"` from left panel section headers and replaced with `data-collapsible="true"` markers.
      - replaced inline logs close `onclick` with `id="logs-close"` button.
    - Kept `index.html` shell path unchanged and maintained legacy fallback safety.
  - Extended tests:
    - `tests/map-service.test.js` now verifies delegation for the new wrapper methods, including popup/orbit, fence set-from-bbox, layer IDs, map/dataLayers compatibility getters, sketch helpers, interaction banner, cancel/highlight helpers.
    - Added widget-engine regression tests:
      - `tests/proximity-join-engine.test.js`
      - `tests/spatial-analyzer-engine.test.js`

## Verification

- **Vitest**: `npm test` — green (103 tests).
- **Build**: `npm run build` — succeeds; emits `dist/`.
- **This slice (M7 points + coord converter)**:
  - After `openPointsWithinPolygon()` migration:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
  - After `openCoordConverter()` migration:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 photo mapper + ArcGIS importer)**:
  - After `openPhotoMapper()` migration:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
  - After `openArcGISImporter()` migration:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
  - After ArcGIS React dialog parity fix (empty custom URL warning path):
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 import/export-adjacent dialogs)**:
  - After Import Fence + NetworkLink + KML/KMZ picker React migrations:
    - First run: `npm test` had one transient timeout in `tests/libs-loader.test.js` (no codepath changes in lib loader).
    - Rerun: `npm test` — green (103 tests).
    - Rerun: `npm run build` — green; emits `dist/`.
- **This slice (M7 draw + merge utility dialogs)**:
  - After `createDrawLayer()` chooser + `handleMergeLayers()` React migrations:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 split/combine data-prep dialogs)**:
  - After `openSplitColumn()` + `openCombineColumns()` React migrations:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 replace/type/deduplicate data-prep dialogs)**:
  - After `openReplaceClean()` + `openTypeConvert()` + `openDeduplicate()` React migrations:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 proximity-join widget React path)**:
  - After `openProximityJoin()` React migration with shared engine extraction:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 bulk-update widget React path)**:
  - After `openBulkUpdate()` React migration:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 spatial-analyzer widget React path)**:
  - After `openSpatialAnalyzer()` React migration with shared engine extraction:
    - `npm test` — green (103 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M7 widget-engine test coverage)**:
  - After adding regression tests for `proximity-join-engine` + `spatial-analyzer-engine`:
    - `npm test` — green (108 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M8 draw-toolbar React path)**:
  - After React draw-toolbar integration in `draw-manager` with legacy fallback:
    - `npm test` — green (108 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M8 import-flow React path)**:
  - After React import-flow modal integration with legacy hidden-input fallback:
    - `npm test` — green (108 tests).
    - `npm run build` — green; emits `dist/`.
  - After routing mobile import actions directly through `openImportFlow()`:
    - `npm test` — green (108 tests).
    - `npm run build` — green; emits `dist/`.
- **This slice (M9 header React path)**:
  - After React header island integration behind `headerReact`:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After wiring primary header actions through React handlers under `headerReact`:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After wiring workflow + basemap + 2D/3D header handlers through React under `headerReact`:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After panel navigation cleanup (shared collapse helpers + no inline `toggleSection` globals):
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After delegated panel tool actions + logs close inline-handler removal:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After delegated output panel action cleanup (`fixAGOL`, export, data preview):
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After legacy layer/field/mobile panel inline-handler removal + delegated rewiring:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After converting additional data-prep/mobile property-based handlers to `addEventListener`:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After converting remaining legacy `.onclick` modal/property handlers in `js/app.js`:
    - `npm test` — green (112 tests).
    - `npm run build` — green; emits `dist/`.
  - After adding event-wiring regression tests:
    - `npm test` — green (114 tests).
    - `npm run build` — green; emits `dist/`.
  - After adding the dual-screen MapService decorator (M10 start):
    - `npm test` — green (117 tests).
    - `npm run build` — green; emits `dist/`.
  - After removing legacy `map-facade` and updating service worker cache list:
    - `npm test` — green (117 tests).
    - `npm run build` — green; emits `dist/`.
  - After dual-screen decorator resize hardening + map-service seam removal:
    - `npm test` — green (117 tests).
    - `npm run build` — green; emits `dist/`.
  - After removing widget-level `mapManager` fallbacks (`bulk-update`, `spatial-analyzer`, `proximity-join`):
    - `npm test` — green (117 tests).
    - `npm run build` — green; emits `dist/`.
- **Dev server smoke**: `npm run dev -- --host 127.0.0.1 --port 4173` starts successfully (Vite ready, local URL printed).
- **Post-migration check**: reran `npm test` + `npm run build` after additional `mapService` call-site migration — still green.
- **Latest extraction check (M4 widgets)**:
  - `npm test` — green (103 tests).
  - `npm run build` — green; emits `dist/`.
  - During this slice, an intermediate build failure (`Identifier "mapService" has already been declared` in `js/widgets/proximity-join.js`) was fixed immediately; final verification is green.
  - Note: in this PowerShell environment, `npm test && npm run build` is not supported (`&&` parsing error), so verification was rerun using `npm test; if ($LASTEXITCODE -eq 0) { npm run build } else { exit $LASTEXITCODE }`.
- **M4 boundary audit**:
  - `rg \bmapManager\b js/` now shows `mapManager` only in:
    - dual-screen overlap modules (`js/dual-screen/dual-screen-map-service.js`, `js/dual-screen/coordinator.js`),
    - legacy adapter source and wrapper: `js/map/map-manager.js`, `js/map/map-service.js`,
    - widget overlap fallbacks (`js/widgets/bulk-update.js`, `js/widgets/spatial-analyzer.js`, `js/widgets/proximity-join.js`) that resolve `mapService` first and keep legacy fallback safety.
- **Checkpoint-ready working tree**:
  - Modified: `js/app.js`, `js/export/exporter.js`, `js/map/draw-manager.js`, `js/map-window.js`, `js/dual-screen/coordinator.js`, `js/dual-screen/layout.js`, `js/dual-screen/secondary-client.js`, `js/map/map-service.js`, `js/widgets/bulk-update.js`, `js/widgets/spatial-analyzer.js`, `js/widgets/proximity-join.js`, `tests/map-service.test.js`, `tests/dual-screen-layout.test.js`, `css/main.css`, `HANDOFF.md`.
  - Added: `js/map/map-feature-flags.js`, `js/ui/left-panel-feature-flags.js`, `js/ui/right-panel-feature-flags.js`, `js/ui/toast-feature-flags.js`, `js/ui/modal-feature-flags.js`, `js/ui/tool-dialog-feature-flags.js`, `react/map/MapView.jsx`, `react/map/mountMapView.jsx`, `react/panels/LeftPanel.jsx`, `react/panels/mountLeftPanel.jsx`, `react/panels/RightPanel.jsx`, `react/panels/mountRightPanel.jsx`, `react/ui/ToastHost.jsx`, `react/ui/mountToastHost.jsx`, `react/ui/ModalHost.jsx`, `react/ui/mountModalHost.jsx`, `react/tools/DistanceToolDialog.jsx`, `react/tools/mountDistanceToolDialog.jsx`, `react/tools/PointToLineDistanceDialog.jsx`, `react/tools/mountPointToLineDistanceDialog.jsx`, `react/tools/BearingToolDialog.jsx`, `react/tools/mountBearingToolDialog.jsx`, `react/tools/DestinationToolDialog.jsx`, `react/tools/mountDestinationToolDialog.jsx`, `react/tools/AlongToolDialog.jsx`, `react/tools/mountAlongToolDialog.jsx`, `react/tools/BufferToolDialog.jsx`, `react/tools/mountBufferToolDialog.jsx`, `react/tools/SimplifyToolDialog.jsx`, `react/tools/mountSimplifyToolDialog.jsx`, `react/tools/ClipExtentDialog.jsx`, `react/tools/mountClipExtentDialog.jsx`, `react/tools/BboxClipDialog.jsx`, `react/tools/mountBboxClipDialog.jsx`, `react/tools/BezierSplineDialog.jsx`, `react/tools/mountBezierSplineDialog.jsx`, `react/tools/PolygonSmoothDialog.jsx`, `react/tools/mountPolygonSmoothDialog.jsx`, `react/tools/LineOffsetDialog.jsx`, `react/tools/mountLineOffsetDialog.jsx`, `react/tools/LineSliceAlongDialog.jsx`, `react/tools/mountLineSliceAlongDialog.jsx`, `react/tools/LineSliceDialog.jsx`, `react/tools/mountLineSliceDialog.jsx`, `react/tools/LineIntersectDialog.jsx`, `react/tools/mountLineIntersectDialog.jsx`, `react/tools/KinksDialog.jsx`, `react/tools/mountKinksDialog.jsx`, `react/tools/CombineFeaturesDialog.jsx`, `react/tools/mountCombineFeaturesDialog.jsx`, `react/tools/UnionPolygonsDialog.jsx`, `react/tools/mountUnionPolygonsDialog.jsx`, `react/tools/DissolveDialog.jsx`, `react/tools/mountDissolveDialog.jsx`, `react/tools/SectorDialog.jsx`, `react/tools/mountSectorDialog.jsx`, `react/tools/NearestPointDialog.jsx`, `react/tools/mountNearestPointDialog.jsx`, `react/tools/NearestPointOnLineDialog.jsx`, `react/tools/mountNearestPointOnLineDialog.jsx`, `react/tools/NearestPointToLineDialog.jsx`, `react/tools/mountNearestPointToLineDialog.jsx`, `tests/map-feature-flags.test.js`, `tests/left-panel-feature-flags.test.js`, `tests/right-panel-feature-flags.test.js`, `tests/toast-feature-flags.test.js`, `tests/toast-events.test.js`, `tests/modal-feature-flags.test.js`, `tests/modal-events.test.js`, `tests/tool-dialog-feature-flags.test.js`.
  - Plus recent slices:
    - `react/tools/NearestNeighborAnalysisDialog.jsx`, `react/tools/mountNearestNeighborAnalysisDialog.jsx`, `react/tools/NearestNeighborResultsDialog.jsx`, `react/tools/mountNearestNeighborResultsDialog.jsx`.
    - `react/tools/PointsWithinPolygonDialog.jsx`, `react/tools/mountPointsWithinPolygonDialog.jsx`, `react/tools/CoordConverterDialog.jsx`, `react/tools/mountCoordConverterDialog.jsx`.
    - `react/tools/PhotoMapperDialog.jsx`, `react/tools/mountPhotoMapperDialog.jsx`, `react/tools/ArcGISImporterDialog.jsx`, `react/tools/mountArcGISImporterDialog.jsx`.
    - `react/tools/ImportFenceOptionsDialog.jsx`, `react/tools/mountImportFenceOptionsDialog.jsx`, `react/tools/NetworkLinksDialog.jsx`, `react/tools/mountNetworkLinksDialog.jsx`, `react/tools/KmlExportPickerDialog.jsx`, `react/tools/mountKmlExportPickerDialog.jsx`.
    - `react/tools/DrawLayerChooserDialog.jsx`, `react/tools/mountDrawLayerChooserDialog.jsx`, `react/tools/MergeLayersDialog.jsx`, `react/tools/mountMergeLayersDialog.jsx`.
    - `react/tools/SplitColumnDialog.jsx`, `react/tools/mountSplitColumnDialog.jsx`, `react/tools/CombineColumnsDialog.jsx`, `react/tools/mountCombineColumnsDialog.jsx`.
    - `react/tools/ReplaceCleanDialog.jsx`, `react/tools/mountReplaceCleanDialog.jsx`, `react/tools/TypeConvertDialog.jsx`, `react/tools/mountTypeConvertDialog.jsx`, `react/tools/DeduplicateDialog.jsx`, `react/tools/mountDeduplicateDialog.jsx`.
    - `js/widgets/proximity-join-engine.js`, `react/tools/ProximityJoinDialog.jsx`, `react/tools/mountProximityJoinDialog.jsx`.
    - `react/tools/BulkUpdateDialog.jsx`, `react/tools/mountBulkUpdateDialog.jsx`.
    - `js/widgets/spatial-analyzer-engine.js`, `react/tools/SpatialAnalyzerDialog.jsx`, `react/tools/mountSpatialAnalyzerDialog.jsx`.
    - `tests/proximity-join-engine.test.js`, `tests/spatial-analyzer-engine.test.js`.
    - `js/map/draw-manager.js`, `react/map/DrawToolbar.jsx`, `react/map/mountDrawToolbar.jsx`.
    - `react/tools/ImportFlowDialog.jsx`, `react/tools/mountImportFlowDialog.jsx`.
    - `js/ui/header-feature-flags.js`, `react/header/HeaderBar.jsx`, `react/header/mountHeaderBar.jsx`, `tests/header-feature-flags.test.js`.
  - Current working tree (exact files changed in this session): `index.html`, `js/app.js`, `js/map/draw-manager.js`, `js/ui/header-feature-flags.js`, `js/widgets/proximity-join-engine.js`, `js/widgets/spatial-analyzer-engine.js`, `react/header/HeaderBar.jsx`, `react/header/mountHeaderBar.jsx`, `react/map/DrawToolbar.jsx`, `react/map/mountDrawToolbar.jsx`, `react/tools/ImportFlowDialog.jsx`, `react/tools/mountImportFlowDialog.jsx`, `react/tools/ProximityJoinDialog.jsx`, `react/tools/mountProximityJoinDialog.jsx`, `react/tools/BulkUpdateDialog.jsx`, `react/tools/mountBulkUpdateDialog.jsx`, `react/tools/SpatialAnalyzerDialog.jsx`, `react/tools/mountSpatialAnalyzerDialog.jsx`, `tests/header-feature-flags.test.js`, `tests/proximity-join-engine.test.js`, `tests/spatial-analyzer-engine.test.js`, `HANDOFF.md`.
  - This continuation slice changed: `js/app.js`, `tests/event-wiring-regression.test.js`, `HANDOFF.md`.
  - This continuation slice changed: `js/app.js`, `js/dual-screen/dual-screen-map-service.js`, `tests/dual-screen-map-service.test.js`, `HANDOFF.md`.
  - This continuation slice changed: `js/app.js`, `js/dual-screen/dual-screen-map-service.js`, `sw.js`, `tests/dual-screen-map-service.test.js`, `HANDOFF.md`; removed `js/dual-screen/map-facade.js`.
  - This continuation slice changed: `js/dual-screen/dual-screen-map-service.js`, `js/map/map-service.js`, `tests/dual-screen-map-service.test.js`, `HANDOFF.md`.
  - This continuation slice changed: `js/widgets/bulk-update.js`, `js/widgets/spatial-analyzer.js`, `js/widgets/proximity-join.js`, `HANDOFF.md`.
  - This continuation slice changed: `js/dual-screen/layout.js`, `tests/dual-screen-layout.test.js`, `tests/dual-screen-primary-handlers.test.js`, `tests/dual-screen-secondary-client.test.js`, `docs/REACT_REFACTOR_PLAN.md`, `docs/DUAL_SCREEN_MODE.md`, `HANDOFF.md`.
  - This continuation slice changed: `js/app.js`, `js/map/map-manager.js`, `js/map/map-feature-flags.js`, `js/ui/header-feature-flags.js`, `js/ui/tool-dialog-feature-flags.js`, `tests/event-wiring-regression.test.js`, `tests/map-feature-flags.test.js`, `tests/header-feature-flags.test.js`, `tests/tool-dialog-feature-flags.test.js`, `react/map/mountMapView.jsx`, `react/panels/mountLeftPanel.jsx`, `react/panels/mountRightPanel.jsx`, `react/ui/mountModalHost.jsx`, `react/ui/mountToastHost.jsx`, `react/workflow/mountPipelineEditor.jsx`, all `react/tools/mount*.jsx` modules, and `HANDOFF.md`; removed `react/bridge.js` and `tests/react-bridge.test.js`.
  - This continuation slice changed: `vite.config.js`, `package.json`, `package-lock.json`, `index.html`, `manifest.json`, `.cursor/environment.json`, `AGENTS.md`, `docs/DUAL_SCREEN_MODE.md`, and `HANDOFF.md`; removed `sw.js`.
  - This continuation slice changed: `vite.config.js`, `HANDOFF.md` (manual chunk tuning to reduce initial chunk size and remove chunk-cycle warnings).
  - This continuation slice changed: `js/workflow/nodes/transform-nodes.js`, `js/workflow/nodes/spatial-nodes.js`, `js/export/kmz-exporter.js`, `js/app.js`, `vite.config.js`, `HANDOFF.md` (normalized dynamic/static imports and tuned manual chunks for stable M12 build output).
  - This continuation slice changed: `js/core/state.js`, `js/app.js`, `vite.config.js`, `HANDOFF.md` (removed remaining dynamic `data-model` imports in state/app hot paths; finalized clean M12 build warnings state).
  - This continuation slice changed: `js/core/state.js`, `react/panels/mountLeftPanel.jsx`, `react/map/mountMapView.jsx`, `react/workflow/mountPipelineEditor.jsx`, `react/mountIsland.jsx`, `AGENTS.md`, `HANDOFF.md` (consolidated undo-system ownership by removing unused legacy transform history exports from `state.js`; cleaned stale bridge/legacy mount comments).
  - This continuation slice changed: `index.html`, `react/header/HeaderBar.jsx`, `js/app.js`, `vite.config.js`, `robots.txt`, `HANDOFF.md` (M12 polish: switched critical UI logo paths to lightweight `favicon.png`, added SEO description/robots metadata, removed `TitleIcon` from PWA includeAssets, and added root `robots.txt`).
  - This continuation slice changed: `index.html`, `js/app.js`, `HANDOFF.md` (accessibility polish: restored zoom-friendly viewport meta and labeled the hidden import file input used by the import flow).
  - This continuation slice changed: `css/main.css`, `HANDOFF.md` (removed `Side_Background.webp` from side-panel rendering path and replaced it with CSS gradients to drop a 2.7MB asset from the shipped build).
- **Notes**:
  - Vite now builds without oversized-chunk warnings, without chunk-cycle warnings, and without dynamic/static overlap warnings.
  - Verification (this slice): `npm test` ✅ (`30` files, `120` tests), then `npm run build` ✅.
  - Lighthouse baseline before this M12 polish (captured earlier in-session): Performance `45`, Accessibility `89`, Best Practices `100`, SEO `50`; key metrics: FCP `3.1s`, LCP `19.4s`, TBT `1240ms`, Speed Index `3.1s`, CLS `0.001`.
  - Lighthouse after logo+SEO polish:
    - command: `npx --yes lighthouse http://127.0.0.1:4173 --quiet --chrome-flags="--headless --no-sandbox --disable-gpu" --output json --output html --output-path "./dist/lighthouse-preview-after"`.
    - report artifacts: `dist/lighthouse-preview-after.report.html`, `dist/lighthouse-preview-after.report.json`.
    - scores: Performance `56`, Accessibility `89`, Best Practices `100`, SEO `92`.
    - key metrics: FCP `3.1s`, LCP `12.0s`, TBT `500ms`, Speed Index `3.1s`, CLS `0.003`.
  - Lighthouse after accessibility fixes:
    - command: `npx --yes lighthouse http://127.0.0.1:4173 --quiet --chrome-flags="--headless --no-sandbox --disable-gpu" --output json --output html --output-path "./dist/lighthouse-preview-latest"`.
    - report artifacts: `dist/lighthouse-preview-latest.report.html`, `dist/lighthouse-preview-latest.report.json`.
    - scores: Performance `56`, Accessibility `100`, Best Practices `100`, SEO `92`.
    - key metrics: FCP `3.1s`, LCP `10.8s`, TBT `540ms`, Speed Index `3.1s`, CLS `0.003`.
  - Lighthouse after dropping the side-panel background image:
    - command: `npx --yes lighthouse http://127.0.0.1:4173 --quiet --chrome-flags="--headless --no-sandbox --disable-gpu" --output json --output html --output-path "./dist/lighthouse-preview-final"`.
    - report artifacts: `dist/lighthouse-preview-final.report.html`, `dist/lighthouse-preview-final.report.json`.
    - scores: Performance `49`, Accessibility `100`, Best Practices `100`, SEO `92`.
    - key metrics: FCP `2.9s`, LCP `12.3s`, TBT `890ms`, Speed Index `2.9s`, CLS `0.003`.
    - note: Lighthouse runs are noisy on local preview; keep `lighthouse-preview-latest` and `lighthouse-preview-final` as comparison points and use median-of-3 for next perf tuning pass.
  - Preview smoke: `npm run preview -- --host 0.0.0.0 --port 4173 --strictPort` starts successfully (dist served, `http://localhost:4173/`).
  - M12 code migration status: `vite-plugin-pwa` is active (`generateSW`), build emits `dist/sw.js` + Workbox runtime and `dist/manifest.webmanifest`; `map-window.html` now ships in `dist` via multi-entry build config.
  - Full M3 **manual browser parity** checklist could not be executed from this agent runtime (no interactive browser control). Needs human/manual pass in-browser.

## Next

1. Run the M11 + M12 manual browser parity pass in `npm run preview` build output:
   - default React shell paths for map/header/tool dialogs,
   - coordinate-search popup actions (add new / add existing / dismiss),
   - workflow editor load/run/add-to-map flow,
   - dual-screen activate/sync/exit/restore,
   - PWA install/offline/update behavior on the Vite-built output.
2. Continue M12 polish:
   - continue Lighthouse improvement from the current baseline (focus next on large image delivery/LCP and remaining SEO crawlability item `is-crawlable`),
   - optionally tune PWA precache/runtime strategy after manual offline/install verification results.
3. Keep `main` shippable per slice with `npm test` + `npm run build` and update this handoff after each batch.

**New agent prompt**:
Continue from this checkpoint in `staging-GIS-toolbox`.
- Read first: `HANDOFF.md`, `AGENTS.md`, `docs/REACT_REFACTOR_PLAN.md`.
- Current state: M10 + M11 code-complete; core M12 migration is applied (`vite-plugin-pwa` in Vite config, legacy `sw.js` removed, dist preview flow configured, map-window included in dist build).
- Immediate next task: perform manual browser PWA/offline/update verification and continue M12 perf/SEO polish using `dist/lighthouse-preview-latest.report.*` as the current benchmark.
- Constraints: keep `main` shippable after each slice.
- After each substantive edit: run `npm test`; if green run `npm run build` (PowerShell-safe sequencing).
- Update `HANDOFF.md` with exact files changed, verification status, and next action.

---

_Archive older bullets when stale (optional):_

- 2026-06-04: Dual Screen activation fix on `main` (#16).
- 2026-06-04: Phase 4 polish merged (#13).
